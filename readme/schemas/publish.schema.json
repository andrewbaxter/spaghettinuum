{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Publish",
  "type": "object",
  "required": [
    "data",
    "missing_ttl"
  ],
  "properties": {
    "data": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/PublishValue"
      }
    },
    "missing_ttl": {
      "description": "duration in minutes",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    }
  },
  "definitions": {
    "PublishValue": {
      "type": "object",
      "required": [
        "data",
        "ttl"
      ],
      "properties": {
        "data": true,
        "ttl": {
          "description": "duration in minutes",
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        }
      }
    }
  }
}